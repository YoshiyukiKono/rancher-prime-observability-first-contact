global:
  suseObservability:
    baseUrl: https://observability.localhost
    license: "<LICENSE_KEY>"
    adminPassword: "<ADMIN_PASSWORD>"

stackstate:
  allowedOrigins:
    - https://rancher.localhost

ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: observability.localhost
